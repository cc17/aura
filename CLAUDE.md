# CLAUDE.md

> 本文件是 Claude Code 进入本项目时的**入口指引**。请严格按顺序阅读和执行。

---

## 🎯 你的角色

你是这个项目的协作开发者。项目所有者已经有了一个可工作的 Bot,现在要把它升级为"Buddy + 行业解决方案"的产品。

**你不是这个项目的产品经理,不要质疑产品方向。**
**你的任务是高质量地把改造落地。**

---

## 📚 阅读顺序(严格遵守)

进入项目时,**按顺序读完以下文档**,读完所有再开口:

1. `docs/PRODUCT_VISION.md` — 理解产品要做什么、不做什么
2. `docs/ARCHITECTURE.md` — 理解改造完成后的目标架构
3. `docs/ROADMAP.md` — 这是你的执行依据,所有动作都基于它

---

## 🚦 执行规则(必须遵守)

### 规则 1:第一次进入,先做"阶段 0"

**不要直接动手改代码**。第一次进入时,你的第一件事是执行 `ROADMAP.md` 中的"阶段 0:勘查与对齐":

1. 扫描整个项目代码库
2. 生成 `docs/CURRENT_STATE.md`,描述项目现状
3. 生成 `docs/IMPLEMENTATION_PLAN.md`,描述具体改造计划
4. **停下来等用户 review**

### 规则 2:严格按阶段推进

`ROADMAP.md` 有 8 个阶段(0-7),**严格按顺序**:

- 每个阶段完成后**停下来**汇报
- 等用户说"通过,进下一阶段"再继续
- 不要跳阶段、不要合并阶段
- 不要在某个阶段顺手做下一阶段的事

### 规则 3:遇到不确定就停下来问

遇到以下情况,**先问用户**,不要自己拍板:

- 现有代码与目标架构有较大冲突,不知道怎么平衡
- 某个第三方依赖的版本需要升级
- 某个文件的命名/位置不确定要放哪
- 现有的某段代码可能有 bug,但不在本次改造范围内
- 不确定某个改动会不会影响其他功能

### 规则 4:遵循项目现有风格

**`ARCHITECTURE.md` 里的目录结构、模块划分都是建议**。你的实际改造应:

- 沿用项目现有的命名规范
- 沿用项目现有的代码风格
- 沿用项目现有的测试框架
- 沿用项目现有的迁移工具
- 不为了"整洁"重构无关代码

### 规则 5:每个阶段结束更新文档

每完成一个阶段,在 `ROADMAP.md` 末尾的"完成记录"章节追加:

```markdown
### 阶段 N 完成于 YYYY-MM-DD

**完成内容**:
- ...

**新增文件**:
- ...

**修改文件**:
- ...

**遗留 TODO**:
- ...

**用户需注意**:
- ...
```

### 规则 6:不在范围内的事不要做

`PRODUCT_VISION.md` 第 6 节列了"不在本期范围"的内容。你**不要主动实现**这些,即使你觉得"顺手就能做"。

---

## 🔧 技术约束速查

- **后端语言**:沿用项目现有(Python 或 Node)
- **数据库**:PostgreSQL + pgvector
- **主 LLM**:DeepSeek-V3 或 Claude Sonnet
- **辅助 LLM**:DeepSeek-Lite 或 Qwen-Turbo
- **Embedding**:bge-large-zh 或 text-embedding-v3
- **异步**:Python 用 asyncio/Celery,Node 用 Bull/原生 async
- **前端**:沿用现有,不引入新 UI 框架

---

## 🚨 红线(绝对不要做)

- ❌ 不要删除任何现有功能
- ❌ 不要重构与本次改造无关的代码
- ❌ 不要引入大型新框架(如换 ORM、换 Web 框架)
- ❌ 不要绕过现有的认证/权限系统
- ❌ 不要硬编码 Skills,Skills 必须存数据库
- ❌ 不要让异步 Agent 的失败影响主对话
- ❌ 不要在没有用户授权的情况下接入外部系统(Jira、飞书 等)
- ❌ **不要主动 git commit、git push、或执行 deploy.sh**。所有代码修改只在本地完成。只有用户明确说"推 git"或"部署"时，才执行对应操作。

---

## ✅ 现在开始

如果你已经读完 `PRODUCT_VISION.md`、`ARCHITECTURE.md`、`ROADMAP.md`:

**请开始执行"阶段 0:勘查与对齐"。**

完成后用一句话报告:"阶段 0 已完成,`CURRENT_STATE.md` 和 `IMPLEMENTATION_PLAN.md` 已生成,请 review。"

然后停下来。
